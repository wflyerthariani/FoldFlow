conda.enabled = true

process {
    executor = 'slurm'
    queue = 'batch'
    cpus = 4
    memory = '32 GB'
    time = '1h'
    clusterOptions = '--gres=gpu:1 --mail-user=arman.thariani@kaust.edu.sa --mail-type=ALL'
}

singularity {
    enabled = true
    autoMounts = true
}